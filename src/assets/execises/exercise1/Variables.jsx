import React from "react";

const Variables = () => {
  const Variables = () => {
    let name = "meenakshi"; // Variable declared using let
    const age = 18; // Constant variable declared using const
    name = "meenakshi"; // You can reassign 'name' since it was declared with 'let'
    // age = 20;          // Error: Assignment to constant variable

    console.log(age);
    console;
    return (
      <div>
        <h1>Variables</h1>
      </div>
    );
  };
  return <div>Variables</div>;
};

export default Variables;
